/* The home page's panels are the shared ones; these names stay so the home
   components keep reading as "home". */
export {
  Panel as HomePanel,
  PanelEmpty,
  PanelFooter,
} from "@/components/shared/panel"
